# Blog

Jekyll handles publishing and static pages. The writing index is progressively enhanced with a locally bundled React build, and the ambient background uses a lightweight canvas layer with a CSS fallback.
